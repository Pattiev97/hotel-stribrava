import { useState } from 'react';
import './style.css';

export const CardDetail = ({ room, opened }) => {
  const [finalPrice, setFinalPrice] = useState(0);

  return <div className="container"></div>;
};
