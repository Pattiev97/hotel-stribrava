import { useState } from 'react';
import './style.css';

export const CardDetail = ({ name, opened }) => {
  const [finalPrice, setFinalPrice] = useState(0);
  
  // const [room, setRoom] = useState(null);

  // if (room === null) {
  //   return <p>Loading...</p>;
  // }


  return <div className="container"></div>;
};
